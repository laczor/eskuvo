export function Store({ api }) { // eslint-disable-line no-unused-vars
	const state = {
	};

	const getters = {
	};

	const mutations = {
	};

	const actions = {
	};

	return { state, getters, mutations, actions };
}
